void test5(int n) {
    int i = 0, j = 0, k = 0, l = 0;
    int res = 0;
    
    i = 0;
    while (i < n) {
        j = 0;
        while (j < n) {
            k = 0;
            while (k < n) {
                l = 0;
                while (l < n) {
                    res = res + i + j + k + l;
                    l = l + 1;
                }
                k = k + 1;
            }
            j = j + 1;
        }
        i = i + 1;
    }
    putint(n);
    putch(10);
    putint(i);
    putch(10);
    putint(j);
    putch(10);
    putint(k);
    putch(10);
    putint(l);
    putch(10);
    putint(res);
    putch(10);
}

int main() {
    int i;
    i = -1;
    while (i < 10) {
        i = i + 1;
        test5(i);
    }
    return 0;
}