int pure(int n) {
    return n * 2 + 1;
}

void test(int n) {
    int i, j, k;
    
    i = n;
    j = 0;
    while (i >= 0) {
        while (i >= 0) {
            break;
            j = j + i;
        }
        j = j + i;
        i = i - 1;
    }
    putint(j);
    putch(10);
}

int main() {
    test(-1);
    test(0);
    test(1);
    test(2);
    test(100);
    test(101);
    return 0;
}